export interface navbarTypes {
    id: number;
    title: string;
    link: string;
};

export interface titleProducts {
    id: number;
    title: string;
};

export interface Advertisement {
    id: number;
    title: string;
    discount: string;
};